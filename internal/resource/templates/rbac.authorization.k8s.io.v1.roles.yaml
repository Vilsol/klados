# name: Role
# description: Namespace-scoped RBAC role
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ""
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - watch
